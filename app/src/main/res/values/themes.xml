<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FCINEMA_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="android:navigationBarColor">?android:attr/colorPrimaryDark</item>
        <item name="android:windowBackground">?android:attr/colorPrimaryDark</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:textColor">@color/primary</item>
        <item name="android:textColorPrimary">@color/primary</item>
        <item name="android:fontFamily">@font/inter</item>

    </style>
    <style name="BottomNavTextStyle">
        <item name="android:textSize">12dp</item> <!-- Adjust the text size as per your preference -->
    </style>
    <style name="FontFamilyApp">
        <item name="android:fontFamily">@font/notable</item> <!-- Adjust the text size as per your preference -->
    </style>
    <style name="FontFamilySolgant">
        <item name="android:fontFamily">@font/merienda_regular</item> <!-- Adjust the text size as per your preference -->
    </style>
</resources>
